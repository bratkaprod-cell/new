# Контекст работы с Devin (для продолжения с любого аккаунта)

Сессии:
- Сессия 1: https://app.devin.ai/sessions/12d6faf7084146f1bf1d381ea5792e61
- Сессия 2: https://app.devin.ai/sessions/e5fb5cdbe6e349009a93919971cb0625
- Сессия 3 (завершена): https://app.devin.ai/sessions/59d45dfd99a941a09cca294db07faf03
- Сессия 4: https://app.devin.ai/sessions/671f888741c34817acd4fbd03f11c753
- Сессия 5: https://app.devin.ai/sessions/5e83df9e97cf44559993ef876edcc9c2

(история диалога хранится в сессиях; при смене аккаунта используйте этот файл как контекст)

## Проект

Статический сайт intelprime.ru (бывш. SiteRescue24) — срочное лечение сайтов от вирусов и
восстановление после взлома (WordPress, Bitrix, OpenCart, MODX, Joomla, самописный PHP).

- Репозиторий: https://github.com/bratkaprod-cell/new
- Рабочая ветка с правками: `devin/1787589000-facts-block-redesign`
- PR в `main`: https://github.com/bratkaprod-cell/new/pull/1 (открыт, не смержен)
- Скачать zip-архив актуальной ветки:
  https://github.com/bratkaprod-cell/new/archive/refs/heads/devin/1787589000-facts-block-redesign.zip

## Как собирать и запускать

- Все страницы (`index.html`, `wordpress.html`, `bitrix.html`, `opencart.html`, `modx.html`,
  `joomla.html`, `php.html`, `privacy.html`, `sitemap.xml`, `robots.txt`, `llms.txt`)
  генерируются из `build.py`: `python build.py`. **HTML руками не править — только build.py.**
- Стили: `assets/style.css`, JS (бургер-меню, модалка, формы, sticky CTA, cookie-баннер):
  `assets/main.js`.
- Локальный просмотр: `python -m http.server 8000` из корня репозитория.

## Архив диалога: что сделано (по сессиям)

### Сессия 1

1. Блок «Кратко: что, за сколько и как быстро» переделан из таблицы в карточки с SVG-иконками
   (`facts_section()` в build.py) + тёмная CTA-панель с ценой «от 6 900 ₽», кнопкой и контактами.
2. Исправлен баг мобильной шапки (бургер уходил за край на 375px).
3. Исправлено «мигание» тега «самая массовая CMS» на карточках CMS.
4. Вставлены логотип IntelPrime (`assets/logo.png`) и favicon (`assets/favicon.png`).

### Сессия 2 (текущая)

1. **Блок «Что будет после вашей заявки» (#process)** полностью переделан: карточки шагов
   с номерами, SVG-иконками, тайм-метками и бейджами доверия; линия прогресса на desktop,
   сетка 2×2 на планшете, вертикальный таймлайн на мобильных; CTA-панель «Сайт лежит прямо
   сейчас?» с кнопками «Спасти сайт — 0 ₽» и Telegram. Проверено на 1366/768/375/320px.
2. **Textolite (CMS в `cms/textolite/`)**: разобрана проблема входа. Пустой `password` в
   conf.ini делает вход невозможным (это не последствие удаления «стучалок» — они
   авторизацию не трогали, вход проверен локально на PHP 8.3). Задан пароль
   **intelprime2026**, его Textolite-хеш записан в `cms/textolite/conf.ini`
   (sha1(sha1(pass)[:22]+pass) + 500 000 итераций SHA-1). На сервере нужно обновить conf.ini.
3. **Форма заявки**: добавлено поле «Ваше имя» (в основную форму и модалку), с валидацией.
4. **Логотип и favicon** заменены на новые изображения от владельца.
5. **Popup по карточкам процесса**: клик (и Enter/Space с клавиатуры) по карточке шага
   открывает модалку заявки (#lead-modal), класс `js-open-modal`.
6. **Соцсети**: ряд иконок (Telegram, WhatsApp, MAX, VK, телефон) добавлен в CTA-панель
   процесса (`proc-socials`, под текстом), у email в блоке контактов иконка конверта
   (`assets/icons/mail.svg`).
7. **Футер**: удалены пункты «Договор-оферта» и «Реквизиты»; починен блок соцсетей
   (ломало CSS-правило иконки email — удалено); у email в футере иконки нет.
8. Убран текст «Обновлено: …» везде (включая llms.txt).
9. **Ребрендинг**: SiteRescue24 → intelprime.ru во всех текстах, титулах, schema.org,
   Open Graph и копирайте. ВНИМАНИЕ: домен в build.py (`DOMAIN = https://siterescue24.ru`)
   и email `help@siterescue24.ru` пока НЕ менялись — ждут решения владельца.
10. **Cookie-баннер**: ненавязчиво всплывает справа внизу через 1,5 сек, кнопка «Принять»,
    ссылка на политику; выбор хранится в localStorage (`ip_cookie_ok`), после принятия
    не показывается. На мобильных растягивается по низу над sticky-CTA.
11. **Страница privacy.html** — политика конфиденциальности по 152-ФЗ (`privacy_page()` в
    build.py): только реально собираемые данные (имя, телефон, URL сайта, email при
    обращении, cookie). Ссылки на неё — в футере и в подписях форм. Юридические реквизиты
    оператора (ИП/ООО, ИНН, адрес) НЕ заполнены — владелец их не предоставил.

### Сессия 3

1. Из meta-тега CSP убрана директива `frame-ancestors` (в `<meta>` она игнорируется браузером
   и сыпала предупреждения в консоль); в `.htaccess` она осталась — заголовок сервер отдаёт корректно.
2. Cookie-баннер на мобильных (<620px) прижат к самому низу экрана (скруглены только верхние углы).
3. Из `facts-cta` убрана текстовая ссылка Telegram (ряд соц-иконок `facts-socials` не тронут).
4. «Ответим за 10 минут» в `facts-cta` выровнено по центру.
5. Блок STRIP («30 мин / 24 часа / 0 ₽ / 1 год») переделан: тёмная панель с бейджем
   «Наши обязательства», заголовком, карточками с иконками и hover-эффектом,
   CTA-кнопкой «Получить бесплатную диагностику» (открывает модалку) и строкой доверия.
   Классы `strip-*` в style.css.
6. Вторая партия правок: hover жёлтым у номера в шапке; новый продающий заголовок блока
   фактов («Сайт снова работает через 4–12 часов — платите только за результат»);
   `facts-socials` по центру; кнопка `facts-btn` ведёт на `index.html#cms`; убраны все
   маркетинговые упоминания «по договору» (юридический текст в privacy.html не тронут);
   блок strip-sec растянут во всю ширину экрана.
7. AEO/AI-видимость: добавлены og:image/twitter:image и logo/image в schema.org.
   Уже было: JSON-LD (@graph: ProfessionalService, WebSite/WebPage+speakable, Service,
   BreadcrumbList, FAQPage), llms.txt, robots.txt с разрешением всех ИИ-ботов.
   ГЛАВНЫЙ блокер ИИ-видимости: DOMAIN всё ещё siterescue24.ru (canonical, sitemap,
   robots, llms.txt ссылаются на старый домен). Решение владельца: пока оставить
   siterescue24.ru.
8. Тексты карточек CMS на главной (блок #cms) переписаны на продающие: у каждой CMS
   своё поле `card` в словаре CMS в build.py (боль/выгода/срок вместо шаблонного
   «… — и другие проблемы. Починим и защитим»). Заодно убраны оставшиеся упоминания
   договора: в sub Битрикса и в бейджах футера (теперь «Без предоплаты»).
9. Маркетинг: собрано семантическое ядро ВЧ/СЧ/НЧ (`docs/semantics-intelprime.xlsx`,
   генератор `docs/gen_semantics.py`; частотности — экспертная оценка, сверить в Wordstat)
   и объявления + стратегия Яндекс.Директа под тестовый бюджет 6 000 ₽
   (`docs/yandex-direct-ads.md`). До запуска рекламы обязательно заменить
   плейсхолдеры контактов и поставить Метрику с целями.
10. В proc-cta («Сайт лежит прямо сейчас?») убрана кнопка «Написать в Telegram»,
    вместо неё текстовый номер телефона (`.proc-cta-phone`, hover жёлтый);
    из ряда соц-иконок этого блока убрана иконка телефона
    (у `socials()` появился параметр `phone=False`).
11. Добавлена мини-форма «Ваш сайт → Отправить» (`quick_form()` в build.py, классы
    `quick-form`/`qf-*`, тёмный вариант `qf-dark`) в 4 местах: под money-блоком
    (на всех страницах), внизу #cms, под «Почему мы, а не мастер с Авито», под тарифами.
    Антиспам как у остальных форм: honeypot, задержка 3 сек после загрузки, валидация URL;
    после отправки открывается модалка заявки с предзаполненным сайтом и фокусом на имени
    (там же rate-limit 3 заявки/10 мин).

### Сессия 4

1. **Реальная доставка заявок**: добавлены `send-lead.php` (обработчик: honeypot,
   rate-limit 5 заявок/10 мин по IP, отправка) и `lead-config.php` (все настройки).
   Каналы: письмо на **fox6261@mail.ru** (PHP mail()), уведомление в Telegram
   (Bot API — нужен токен бота и chat_id, см. комментарии в lead-config.php),
   уведомление в VK на id292590707 (messages.send от имени сообщества — нужен
   ключ сообщества). **Все контакты/токены меняются только в `lead-config.php`.**
   `assets/main.js`: обе формы (основная и модалка) теперь шлют fetch POST
   на send-lead.php; при ошибке показывают текст и не блокируют кнопку.
   Требуется PHP на хостинге (как у Textolite). Пока TG_BOT_TOKEN/VK_TOKEN пустые —
   уходит только email.
2. Из header-соцсетей (nav-socials, desktop) убрана иконка телефона; мобильная
   иконка `nav-phone-icon` (<560px) оставлена.
3. В proc-cta кнопка «Спасти сайт — 0 ₽» → «Узнать диагноз за 30 минут — 0 ₽»
   (кнопки Telegram там уже не было — убрана в сессии 3).
4. В контакт-блоке (#contact): из form-socials убрана иконка телефона,
   из .messengers убрана кнопка «Написать в Telegram» (остался только номер).

Диалог сессии 4 (кратко):
- Владелец: подключить заявки не в Telegram-бот, а на почту fox6261@mail.ru,
  плюс уведомления в Telegram (https://t.me/intel_prime) и в VK
  (https://vk.ru/id292590707); дать знать, в каком файле потом менять данные.
  Ответ: всё меняется в `lead-config.php`; по прямым ссылкам t.me/VK слать
  нельзя (устройство их API) — нужны токен бота Telegram (+chat_id) и ключ
  сообщества VK, инструкции в комментариях lead-config.php. Пока токены пустые —
  уведомления уходят только на почту.
- Владелец: убрать на десктопе иконку телефона в хедере; убрать кнопку
  «Написать в Telegram» из proc-блока и придумать более конверсионный текст
  для «Спасти сайт — 0 ₽»; в контакт-блоке убрать иконку телефона и кнопку
  «Написать в Telegram». Всё сделано (кнопки TG в proc-блоке уже не было —
  убрана в сессии 3; новый текст CTA — «Узнать диагноз за 30 минут — 0 ₽»).
- Владелец: сохранить весь диалог и файлы на GitHub для продолжения
  с другого аккаунта — этот файл обновлён и запушен в рабочую ветку.

### Сессия 4 — продолжение (домен, контакты, меню)

5. **Домен**: владелец подтвердил, что адрес сайта — **https://intel-prime.ru** (с дефисом).
   `DOMAIN` в build.py заменён; canonical, sitemap.xml, robots.txt, llms.txt, schema.org,
   privacy.html — везде intel-prime.ru. Email оставлен help@intelprime.ru (по указанию владельца).
6. **MAX**: ссылка заменена на точный профиль владельца:
   https://max.ru/u/f9LHodD0cOK1e9zbvqqBwfyp-v51tQbrtZB05QeizJUBPKNbS9ZXcv-oo0c
   (точное значение — константа MAX в build.py).
7. Все контактные ссылки (Telegram, WhatsApp, MAX, VK, футерная кнопка Telegram)
   открываются в новой вкладке (`target="_blank" rel="noopener"`).
8. **Меню (header) переработано**: пункт «Цены — от 6 900 ₽» (класс `nav-hot`, жёлтый),
   hover-подсветка пунктов + жёлтое подчёркивание (`.nav-links a::after`).
9. **Адаптивное меню «как на Tilda»** (по просьбе владельца, т.к. на 1180px меню пряталось):
   размеры через CSS `clamp()` (gap, padding, font-size масштабируются от vw).
   Брейкпоинты: ≤1280px номер телефона сворачивается в зелёную иконку (`nav-phone-icon`);
   ≤1150px компактнее CTA и соц-иконки; ≤1000px соц-иконки скрываются; ≤900px — бургер
   (раньше был 1180px). Меню стоит вплотную к логотипу (по замечанию владельца),
   свободное место уходит вправо (`.nav-links~.nav-socials{margin-left:auto}`).
   Проверено Playwright на ширинах 1920–375px — переполнений нет.

Диалог сессии 4, часть 2 (кратко):
- Владелец: заменить ссылку MAX на новую; все контакты — в новом окне; адрес сайта везде
  на https://intel-prime.ru (email не трогать); сделать красивее/конверсионнее меню. Сделано.
- Владелец: «меню скрывается на 1180px — исправь, попробуй как на tilda.cc, чтобы текст
  подстраивался под экран». Сделано через clamp(), бургер теперь с 900px.
- Владелец: «отступы от логотипа слишком большие» — меню придвинуто к логотипу.
- Владелец: «соц-иконки слишком рано исчезают» — теперь видны до 1000px.

Замечание: iframe `chrome-extension://.../inject.html` в DOM — вставка браузерного
расширения владельца, в коде сайта его нет.

### Сессия 5 (адаптивность «как на Tilda»)

1. Полная fluid-адаптивность всего сайта (assets/style.css): размеры текста, кнопок,
   отступов секций/карточек/форм/модалки/футера переведены на CSS `clamp()` —
   всё плавно масштабируется под любую ширину экрана (320px–1920px+), как на Tilda.
   На мобильных (<560px) кнопки hero/footer-CTA/messengers растягиваются на всю ширину.
   Проверено Playwright на 13 ширинах (320–1920px) — горизонтальных переполнений нет.
2. Вторая итерация: у кнопок с фиксированным размером текста (.nav .btn, .strip-btn,
   .facts-btn, .vs-btn, .qf-row .btn, .cms-card .go, .cookie-btn) размер переведён на
   clamp() — теперь текст всех кнопок реально уменьшается на узких экранах;
   кнопка модалки на <380px компактнее (текст в 2 строки вместо 3).
   Полный аудит: 8 страниц × 18 ширин (320–1920px), Playwright — переполнений страницы
   нет, тексты кнопок не обрезаются (замерены computed font-size на 320/768/1920px).

3. Постраничное семантическое ядро: `docs/semantics-pages-intelprime.xlsx`
   (генератор `docs/gen_semantics_pages.js`, node + exceljs). 6 групп по CMS-страницам
   (WordPress/Битрикс/OpenCart/MODX/Joomla/PHP), ~195–212 ключей в каждой, всего 1215.
   Колонки: запрос, частотность (экспертная оценка Wordstat РФ — сверить выборочно),
   конкуренция, теплота, тип, приоритет, комментарий; листы минус-слов.

Диалог сессии 5 (кратко):
- Владелец: «адаптируй весь сайт и кнопки как на tilda.cc — чтобы всё подстраивалось
  под любой экран, и текст, и кнопки». Сделано через clamp() (п.1), затем по замечанию
  «текст кнопок не уменьшается» — вторая итерация (п.2) с полным Playwright-аудитом.
- Владелец: «подготовь семантику по вордстату, постранично по 6 страницам, минимум
  150 ключей на группу (900+ всего), в Excel, как профессиональный директолог,
  на основе конкурентов». Сделано (п.3): 1215 ключей в 6 группах. ВАЖНО: прямого API
  Wordstat нет — частотности являются экспертной оценкой (широкое соответствие, РФ,
  округление до 5) на основе аналогичных проектов (Ревизиум, SiteSecure и т.п.);
  топ-запросы каждой группы перед запуском Директа сверить в wordstat.yandex.ru.
  Регенерация файла: `node docs/gen_semantics_pages.js` (нужен npm-пакет exceljs).
- Владелец: «сохрани полностью диалог, весь контекст» — этот файл обновлён и запушен.
- Владелец: «что за хрень в консоли Chrome — Blocked aria-hidden…». Исправлено (commit c8a6bc6):
  модалка (#lead-modal) и мобильное меню (#mobile-menu) получили атрибут `inert` в build.py,
  а main.js при открытии/закрытии переключает inert и aria-hidden и снимает фокус (blur)
  перед скрытием. Проверено Playwright — предупреждений в консоли 0.
  Строки про mixpanel-2.js — от браузерного расширения владельца, к коду сайта отношения не имеют.
- Владелец: «выложи весь диалог и проект на GitHub, чтобы продолжить с другого аккаунта» —
  всё запушено в ветку devin/1787589000-facts-block-redesign (PR #1 открыт, не смержен).
- Владелец: «сверяй всю частотность и настраивай полностью рекламную кампанию, без слива
  бюджета, подготовь всё для загрузки в рекламный кабинет». Сделано:
  1) Выполнен вход в Яндекс ID (аккаунт Intel Prime, intel-prime-corporation) по номеру
     владельца с кодами из СМС/push; сессия сохранена в браузере машины.
  2) Реальная сверка Wordstat (все регионы, период 31.07–29.08.2026) — 67 масок через
     Playwright-скрипты; сырые данные: `docs/wordstat-data.json`, `docs/wordstat-data2.json`.
     ГЛАВНЫЙ ВЫВОД: ниша низкочастотная. Реальные объёмы: «взломали сайт что делать» 323,
     «на сайте вирусы что делать» 260, «вредоносный код на сайте» 235, «лечение сайтов
     москва» 993, «защита wordpress» 105, «битрикс безопасность» 168, «joomla вирус» 70;
     большинство CMS-ключей 0–50 показов/мес. Широкие маски («проверка сайта на вирусы»
     8804, «безопасность сайта» 40532, «взломали сайт» 9480) на 90–95 % состоят из мусора
     (онлайн-сканеры, «как взломать», скачивание игр, санатории для «лечение сайта») —
     в кампанию не включены. Прежние экспертные частотности из semantics-файлов реальными
     данными НЕ подтверждаются — для Директа использовать только новый файл кампании.
  3) Кампания Яндекс Директ: `docs/direct-campaign-intelprime.xlsx`
     (генератор `docs/gen_direct_campaign.js`, node + exceljs из NODE_PATH).
     2 кампании (Горячие / CMS), 10 групп, 60 фраз (только реальные и околоцелевые,
     с фразовыми минусами -как и т.п.), объявления с проверкой лимитов (З1≤56, З2≤30,
     текст≤81), быстрые ссылки на реальные якоря (#pricing/#process/#faq/#contact),
     уточнения, UTM (utm_campaign=hot/cms, utm_content=<группа>, utm_term={keyword}),
     101 минус-слово на уровень кампании (проверено на отсутствие конфликтов со своими
     ключами — «как»/«что»/«взломать» намеренно НЕ в списке), листы: настройки кампаний
     (стратегия «Максимум кликов», недельный бюджет 1500 ₽/кампания, автотаргетинг только
     «целевые запросы», гео Россия, корректировка <18 лет −100 %), подтверждённые
     частотности, пошаговая инструкция импорта через Директ Коммандер.
  4) НЕ сделано (нужно до запуска): цели в Метрике (форма/телефон/мессенджеры),
     проверка кабинета Директа, модерация, первая неделя — ежедневная чистка
     поисковых запросов. Кампании НЕ запущены — только подготовлены файлы.
- Владелец: «дай ссылку на скачивание» — отправлена ссылка на xlsx (вложение Devin +
  raw-ссылка на GitHub).
- Владелец: «сохрани весь диалог для работы с другого аккаунта и пришли инструкцию» —
  этот файл обновлён и запушен; инструкция отправлена в чат (и продублирована ниже).

### Сессия 6 (загрузка кампаний в Директ)

Сессия: https://app.devin.ai/sessions/2d63a35de4854e7092875f35f5d7d6db

1. Вход в Яндекс выполнен под логином **promalpsmol** (Винокуров Денис,
   тот же телефон 89082857007, код из push/СМС прислал владелец). Кампании
   загружены именно в этот кабинет (баланс на момент загрузки 6 215,73 ₽).
2. Установлен Директ Коммандер (Windows), выполнен вход через OAuth.
3. Импортирован `docs/direct-campaign-intelprime.xlsx` (лист «Объявления и фразы»):
   2 кампании, 10 групп, 60 фраз. Дубли объявлений внутри групп (по одному на фразу)
   удалены — оставлено 1 объявление на группу (10 объявлений), иначе Коммандер
   ругался на лимит комбинаторных объявлений ЕПК.
4. Настройки, заданные в Коммандере для обеих кампаний: названия из файла;
   места показа — только «Продвижение в поиске» (РСЯ, товарная галерея, карты — выкл.);
   стратегия «Максимум кликов», недельный бюджет 1 500 ₽/кампания; расписание 24/7;
   минус-слова (101 шт., одной строкой) на уровне кампаний; корректировка
   «младше 18 лет −100 %»; мониторинг сайта включён. На уровне групп: гео — Россия,
   автотаргетинг только «целевые запросы» (свой бренд + без упоминания бренда,
   конкуренты выкл.).
5. Кампании отправлены на сервер как **ЧЕРНОВИКИ** (не на модерацию):
   - № 714043997 — IntelPrime | Поиск | Горячие (взлом и вирусы)
   - № 714044004 — IntelPrime | Поиск | CMS
   До запуска: цели в Метрике (форма/телефон/мессенджеры), заполнить визитку
   (Яндекс Бизнес, «данные организации неполные»), затем «запустить» → модерация.
   Ставки из колонки «Ставка» файла не применялись (стратегия автоматическая).

### Сессия 7 (аудит и правка загруженных кампаний)

Полный отчёт: `docs/direct-audit-session7.md`. Фактическая выгрузка из Коммандера
(после правок, повторно загружена на сервер): `docs/direct-live-export.csv`
(UTF-16, TSV, 2 служебные строки перед заголовком).

1. Исправлено и выгружено на сервер (кампании остались **черновиками**):
   лимит цены клика 120 ₽ (было не задано); отключено «автоматически применять
   рекомендации»; быстрые ссылки CMS-объявлений переведены с `index.html#…` на свою
   CMS-страницу (`wordpress.html#…` и т. д.); добавлено 34 ключевые фразы
   (блокировки браузером/антивирусом, редиректы, майнер, «цена/заказать», пентест,
   «взломали/удалить вирус» по каждой CMS). Итого 93 фразы + 10 автотаргетингов = 103.
2. Проверено верно: только Поиск; гео Россия; 24/7; <18 лет −100 %; мониторинг сайта;
   автотаргетинг только «целевые запросы» (конкуренты выкл.); 81 минус-слово на уровне
   обеих кампаний (Директ склеил словоформы из 101), конфликтов с ключами — 0;
   UTM с `{keyword}` во всех ссылках. Расхождение 60→59 фраз при первой загрузке —
   Директ склеил дубль `что делать если взломали сайт` = `взломали сайт что делать`.
3. Осталось до запуска (нужен владелец): цели в Метрике (форма, телефон, мессенджеры)
   и привязка счётчика; визитка Яндекс Бизнес («данные организации неполные»);
   решение по бюджету — 1 500 ₽/нед × 2 ≈ 13 000 ₽/мес при балансе 6 215 ₽.
4. Рекомендации на потом: 2–3 заголовка и 2 текста в каждом объявлении ЕПК; уточнения
   («Гарантия 1 год», «Без предоплаты», «Диагностика бесплатно»); отчёт по поисковым
   запросам каждые 2–3 дня; стратегия по конверсиям — только после ~10 конверсий/нед.

## Как продолжить работу с другого аккаунта Devin

1. В новой сессии Devin первым сообщением дать ссылку на этот файл:
   https://github.com/bratkaprod-cell/new/blob/devin/1787589000-facts-block-redesign/docs/devin-context.md
   и написать: «Прочитай этот файл — это контекст проекта. Все файлы в ветке
   devin/1787589000-facts-block-redesign (PR #1). Продолжаем работу: <задача>».
2. Убедиться, что у нового аккаунта есть доступ к репозиторию
   https://github.com/bratkaprod-cell/new (владелец: bratkaprod-cell).
3. Правила для новой сессии: HTML не править руками — только build.py (`python build.py`);
   Excel-файлы генерировать через node + exceljs (`docs/gen_semantics_pages.js`,
   `docs/gen_direct_campaign.js`); коммитить в ветку devin/1787589000-facts-block-redesign.
4. Вход в Яндекс (Wordstat/Директ/Метрика) в новой сессии придётся выполнить заново:
   логин — номер владельца 89082857007, аккаунт Intel Prime (intel-prime-corporation),
   коды приходят по СМС/push — владелец присылает их в чат. Коды и пароли в файлы
   и репозиторий НЕ сохранять.
5. Ближайшие задачи для продолжения: цели в Яндекс Метрике (форма, клик по телефону,
   переход в Telegram/WhatsApp), затем импорт docs/direct-campaign-intelprime.xlsx
   в Директ Коммандер по листу «Инструкция по загрузке», модерация и тестовый запуск
   (бюджет 6 000 ₽/мес, кампании «Горячие» — 2/3 бюджета).

## Коммерческие данные сайта

Диагностика бесплатно · ответ за 10 минут · срок 4–12 ч (макс. 24 ч) · без предоплаты ·
цена от 6 900 ₽ · подписка «Охрана» 1 990 ₽/мес · гарантия 1 год письменно · заявки 24/7 ·
вся Россия и СНГ, удалённо. Контакты в build.py — реальные (сессия 5): +7 900 219-19-70,
t.me/intel_prime, wa.me/79082857007, vk.com/intel_prime, MAX — личная ссылка max.ru/u/… (см. build.py).
Домен сайта: https://intel-prime.ru. Email: help@intelprime.ru.

## Открытые вопросы / будущие задачи

- Доставка заявок подключена (email); для Telegram/VK-уведомлений владелец должен
  вписать токены в `lead-config.php` (инструкция в комментариях файла).

- Заменить плейсхолдеры контактов на реальные (телефон, Telegram, WhatsApp, MAX, VK).
- Домен: владелец подтвердил intelprime.ru — в сессии 5 `DOMAIN` и email заменены на intelprime.ru
  (canonical, sitemap, robots, llms.txt, schema.org, LEAD_EMAIL_FROM).
- Вписать юридические реквизиты оператора в privacy.html (через build.py).
- Смержить PR #1 в main, чтобы всё попало в основную ветку.
- В `cms/textolite/` лежит CMS Textolite; аудит безопасности — `docs/textolite-audit.md`,
  общий security-обзор — `docs/security-review-site-and-cms.md`. На продакшене закрыть
  прямой HTTP-доступ к conf.ini и backup/, пускать CMS только по HTTPS.
- Возможная будущая задача: своя лёгкая CMS для правки текстов/картинок/цветов без кода
  (хранение в JSON + интеграция с build.py или PHP-рантайм под Apache/.htaccess).
