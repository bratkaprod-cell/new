# Контекст работы с Devin (для продолжения с любого аккаунта)

Сессии:
- Сессия 1: https://app.devin.ai/sessions/12d6faf7084146f1bf1d381ea5792e61
- Сессия 2 (актуальная): https://app.devin.ai/sessions/e5fb5cdbe6e349009a93919971cb0625

(история диалога хранится в сессиях; при смене аккаунта используйте этот файл как контекст)

## Проект

Статический сайт intelprime.ru (бывш. SiteRescue24) — срочное лечение сайтов от вирусов и
восстановление после взлома (WordPress, Bitrix, OpenCart, MODX, Joomla, самописный PHP).

- Репозиторий: https://github.com/bratkaprod-cell/new
- Рабочая ветка с правками: `devin/1787589000-facts-block-redesign`
- PR в `main`: https://github.com/bratkaprod-cell/new/pull/1 (открыт, не смержен)
- Скачать zip-архив актуальной ветки:
  https://github.com/bratkaprod-cell/new/archive/refs/heads/devin/1787589000-facts-block-redesign.zip

## Как собирать и запускать

- Все страницы (`index.html`, `wordpress.html`, `bitrix.html`, `opencart.html`, `modx.html`,
  `joomla.html`, `php.html`, `privacy.html`, `sitemap.xml`, `robots.txt`, `llms.txt`)
  генерируются из `build.py`: `python build.py`. **HTML руками не править — только build.py.**
- Стили: `assets/style.css`, JS (бургер-меню, модалка, формы, sticky CTA, cookie-баннер):
  `assets/main.js`.
- Локальный просмотр: `python -m http.server 8000` из корня репозитория.

## Архив диалога: что сделано (по сессиям)

### Сессия 1

1. Блок «Кратко: что, за сколько и как быстро» переделан из таблицы в карточки с SVG-иконками
   (`facts_section()` в build.py) + тёмная CTA-панель с ценой «от 6 900 ₽», кнопкой и контактами.
2. Исправлен баг мобильной шапки (бургер уходил за край на 375px).
3. Исправлено «мигание» тега «самая массовая CMS» на карточках CMS.
4. Вставлены логотип IntelPrime (`assets/logo.png`) и favicon (`assets/favicon.png`).

### Сессия 2 (текущая)

1. **Блок «Что будет после вашей заявки» (#process)** полностью переделан: карточки шагов
   с номерами, SVG-иконками, тайм-метками и бейджами доверия; линия прогресса на desktop,
   сетка 2×2 на планшете, вертикальный таймлайн на мобильных; CTA-панель «Сайт лежит прямо
   сейчас?» с кнопками «Спасти сайт — 0 ₽» и Telegram. Проверено на 1366/768/375/320px.
2. **Textolite (CMS в `cms/textolite/`)**: разобрана проблема входа. Пустой `password` в
   conf.ini делает вход невозможным (это не последствие удаления «стучалок» — они
   авторизацию не трогали, вход проверен локально на PHP 8.3). Задан пароль
   **intelprime2026**, его Textolite-хеш записан в `cms/textolite/conf.ini`
   (sha1(sha1(pass)[:22]+pass) + 500 000 итераций SHA-1). На сервере нужно обновить conf.ini.
3. **Форма заявки**: добавлено поле «Ваше имя» (в основную форму и модалку), с валидацией.
4. **Логотип и favicon** заменены на новые изображения от владельца.
5. **Popup по карточкам процесса**: клик (и Enter/Space с клавиатуры) по карточке шага
   открывает модалку заявки (#lead-modal), класс `js-open-modal`.
6. **Соцсети**: ряд иконок (Telegram, WhatsApp, MAX, VK, телефон) добавлен в CTA-панель
   процесса (`proc-socials`, под текстом), у email в блоке контактов иконка конверта
   (`assets/icons/mail.svg`).
7. **Футер**: удалены пункты «Договор-оферта» и «Реквизиты»; починен блок соцсетей
   (ломало CSS-правило иконки email — удалено); у email в футере иконки нет.
8. Убран текст «Обновлено: …» везде (включая llms.txt).
9. **Ребрендинг**: SiteRescue24 → intelprime.ru во всех текстах, титулах, schema.org,
   Open Graph и копирайте. ВНИМАНИЕ: домен в build.py (`DOMAIN = https://siterescue24.ru`)
   и email `help@siterescue24.ru` пока НЕ менялись — ждут решения владельца.
10. **Cookie-баннер**: ненавязчиво всплывает справа внизу через 1,5 сек, кнопка «Принять»,
    ссылка на политику; выбор хранится в localStorage (`ip_cookie_ok`), после принятия
    не показывается. На мобильных растягивается по низу над sticky-CTA.
11. **Страница privacy.html** — политика конфиденциальности по 152-ФЗ (`privacy_page()` в
    build.py): только реально собираемые данные (имя, телефон, URL сайта, email при
    обращении, cookie). Ссылки на неё — в футере и в подписях форм. Юридические реквизиты
    оператора (ИП/ООО, ИНН, адрес) НЕ заполнены — владелец их не предоставил.

Замечание: iframe `chrome-extension://.../inject.html` в DOM — вставка браузерного
расширения владельца, в коде сайта его нет.

## Коммерческие данные сайта

Диагностика бесплатно · ответ за 10 минут · срок 4–12 ч (макс. 24 ч) · без предоплаты ·
цена от 6 900 ₽ · подписка «Охрана» 1 990 ₽/мес · гарантия 1 год письменно · заявки 24/7 ·
вся Россия и СНГ, удалённо. Контакты в build.py — плейсхолдеры (+7 900 000-00-00,
t.me/your_tg, wa.me/79000000000, max.ru/your_max, vk.com/your_vk), заменить на реальные.

## Открытые вопросы / будущие задачи

- Заменить плейсхолдеры контактов на реальные (телефон, Telegram, WhatsApp, MAX, VK).
- Решить: менять ли `DOMAIN` и email на intelprime.ru (влияет на canonical, sitemap, robots).
- Вписать юридические реквизиты оператора в privacy.html (через build.py).
- Смержить PR #1 в main, чтобы всё попало в основную ветку.
- В `cms/textolite/` лежит CMS Textolite; аудит безопасности — `docs/textolite-audit.md`,
  общий security-обзор — `docs/security-review-site-and-cms.md`. На продакшене закрыть
  прямой HTTP-доступ к conf.ini и backup/, пускать CMS только по HTTPS.
- Возможная будущая задача: своя лёгкая CMS для правки текстов/картинок/цветов без кода
  (хранение в JSON + интеграция с build.py или PHP-рантайм под Apache/.htaccess).
