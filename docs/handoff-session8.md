# Handoff: продолжение работы с нового аккаунта Devin (после сессии 8)

Скопируй первый раздел в стартовое сообщение новой сессии — этого достаточно для продолжения.

## Стартовое сообщение для новой сессии (скопировать целиком)

> Репозиторий bratkaprod-cell/new, ветка `devin/1787589000-facts-block-redesign` (PR #1).
> Прочитай по порядку: `docs/handoff-session8.md` (этот файл — текущее состояние и что делать),
> `docs/direct-keywords-audit-session8.md` (аудит семантики), `docs/devin-context.md` (общий контекст),
> `docs/direct-audit-session7.md`. Вход в Яндекс: логин promalpsmol, телефон 89082857007, коды из
> СМС/push пришлю в чат. Кампании Директа № 714043997 («Горячие») и № 714044004 («CMS») — ЧЕРНОВИКИ,
> не запускать и не отправлять на модерацию без моего слова. HTML сайта генерируется `build.py`,
> руками не править. Продолжи с пункта «Что осталось» из handoff.

## Что уже сделано (по состоянию на 2026-09-01)

Сайт (ветка PR #1, всё запушено):
- Метрика: счётчик 112155075 подключён (`assets/metrika.js`, CSP в `.htaccess` и `build.py`),
  событие формы `lead_form` в `assets/main.js`. Коммит `1382aba`.
- Цели в Метрике: форма (ID 604672012, `lead_form`), клик по телефону (604672139),
  переход в Telegram/WhatsApp (604672295).

Директ (кабинет promalpsmol, обе кампании — черновики):
- Счётчик 112155075 и 3 цели привязаны к обеим кампаниям.
- Минус-слова: в обеих кампаниях список расширен с 81 до 186 слов (медицина/санатории,
  учреждения, бренды антивирусов, «по ссылке/файл/сертификат», учёба и т.д. — полный перечень в
  `docs/direct-keywords-audit-session8.md`, п. 3). В «Горячие» дополнительно добавлена
  кросс-минусовка `-wordpress -битрикс -bitrix -joomla -opencart -modx` (итого 192).
- Ключевые фразы, кампания «Горячие» — ДОБАВЛЕНЫ во все 4 группы:
  - Взломали сайт: 10 → 14; Вирус на сайте: 15 → 34; Лечение сайтов (услуга): 9 → 16;
    Защита и аудит: 9 → 12. Списки фраз — `docs/direct-keywords-audit-session8.md`, п. 2.
- Ключевые фразы, кампания «CMS» — добавлены только в 2 группы: Самописный PHP (6 → 9),
  MODX (7 → 10).

Яндекс Бизнес:
- Организация IntelPrime создана (ID 190166285504), номер +7 900 219-19-70 подтверждён,
  сайт/телефон/email/Telegram/WhatsApp/часы 24/7/рубрики заполнены, изменения на проверке.

## Что осталось (по порядку)

1. Директ, кампания CMS (№ 714044004) — добавить фразы в 4 группы через
   Кампании → Группы → колонка «Ключевые фразы» → «Добавить» → «Добавить фразу», вставить список:
   - OpenCart: `opencart взломали что делать`, `opencart вредоносный код`, `ocstore взлом -!как`
   - Joomla: `joomla взломали восстановить`, `joomla вредоносный код`
   - 1С-Битрикс: `1с битрикс вирус лечение`, `битрикс проактивная защита взлом`, `bitrix вредоносный код`
   - WordPress: `wordpress вредоносный код удалить`, `wp вирус редирект`, `wordpress сайт перекидывает`,
     `wordpress взломали восстановить`, `wordpress malware удалить`
2. Директ — правки существующих фраз (вкладка «Ставки и фразы»):
   - «Горячие», группа «Лечение сайтов (услуга)»: удалить голую фразу `лечение сайтов`
     (сливает бюджет на «лечение официальный сайт», 21 000/мес) — замена уже добавлена.
   - «Горячие», группа «Вирус на сайте»: отключить/удалить `антивирус блокирует сайт`,
     `браузер блокирует сайт вирус`, `сайт помечен как вредоносный`, `сайт редиректит на другой сайт`
     (запросы посетителей, не владельцев; уточнённые замены уже добавлены).
   - «CMS»: к `взлом opencart` и `взлом modx` добавить операторы `-!как -самостоятельно`.
3. Директ — объявления ЕПК: в каждое из 10 объявлений добавить 2–3 заголовка, 2 текста,
   уточнения «Гарантия 1 год», «Без предоплаты», «Диагностика бесплатно».
4. Директ — привязать организацию Яндекс Бизнеса (ID 190166285504) к обеим кампаниям
   (Настройки кампании → Организация), когда пройдёт проверка.
5. Яндекс Бизнес — дописать описание организации.
6. Бюджет: владелец хотел 1 000 ₽/нед «Горячие» + 500 ₽/нед «CMS» при балансе 6 215 ₽, но Директ
   требует минимум 3 000 ₽/нед на кампанию. Нужно решение владельца (например, запустить только
   «Горячие» с 3 000 ₽/нед). Автотаргетинг на первую неделю лучше выключить.
7. Запуск → модерация — ТОЛЬКО после явного «запускай» от владельца.
8. Через 2–3 дня после старта: Статистика → Мастер отчётов → срез «Поисковый запрос»,
   мусор в минусы; на стратегию по конверсиям — после ~10 конверсий/нед.
9. Обновить `docs/devin-context.md` и этот файл, закоммитить в ветку.

## Технические нюансы

- Вход в Яндекс: код приходит push в Яндекс Браузер или СМС — просить у владельца в чат.
- Веб-интерфейс Директа быстрее Коммандера для добавления фраз: окно «Ключевые фразы» принимает
  список по одной фразе в строке; минус-слова кампании — Настройки кампании → Дополнительные
  настройки → «Минус-фразы» (текстовое поле, по одному слову в строке). Кириллицу вставлять из
  буфера обмена (`Set-Clipboard` из UTF-8 файла), не печатать.
- Кнопку «Запустить кампанию» на странице настроек НЕ нажимать; сохранять только «Сохранить изменения».
- Директ Коммандер установлен (OAuth). Выгрузка кабинета — UTF-16 TSV с двумя служебными строками
  перед заголовком; колонки английские (`Campaign name`, `Group name`,
  `Phrase (with negative keywords)`, `Negative keywords for campaign`).
- Последняя выгрузка до правок сессии 8: `docs/direct-live-export.csv` (43 + 50 фраз, 81 минус).
  После завершения п. 1–2 нужно выгрузить заново и заменить файл.
- Сайт: `python build.py` для сборки, локально `python -m http.server 8000`.
